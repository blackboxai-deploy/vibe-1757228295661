import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import Header from '@/components/Header'
import Footer from '@/components/Footer'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Legal Advisory Services - Professional Legal Consultation',
  description: 'Comprehensive legal advisory services covering criminal law, civil law, family law, corporate law, real estate, employment, immigration, and constitutional law. Get expert legal guidance and answers to your legal questions.',
  keywords: 'legal advice, lawyer, attorney, legal consultation, law firm, legal services',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={`${inter.className} min-h-screen flex flex-col`}>
        <Header />
        <main className="flex-1">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  )
}